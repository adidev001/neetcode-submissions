class Solution {
public:
    int shipWithinDays(vector<int>& weights, int days) {
        int r=accumulate(weights.begin(),weight.end(),0);
        int l=max_element(weight.begin(),weights.end());

        int res=r;

        while(l<=r){
            int mid=(l+r)/2;
            if( sail(weight,days,mid)){
                res= min(res,cap)
            }
        }



        
    }
};